var numTrees = function(n) {
    
    var C = 1; //initital catalan number C(0)

    for(var i=1 ; i<n ; i++){ //if n= 3, then i will be 1,2
   
        C = 2*(2*i+1)/(i+2) * C; //Catalan Numbers for unique BST!
                
    }
    
    return C;
};
